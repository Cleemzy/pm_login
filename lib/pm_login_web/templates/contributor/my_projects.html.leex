<h1><i class="bi bi-kanban"></i> Les projets à qui on m'a assigné des tâches</h1>

<div class="">

  <ul>
    <%= for project <- @projects do %>
      <li><%= link "#{project.title}", to: Routes.project_path(@socket, :board, project) %></li>
    <% end %>
  </ul>

</div>
