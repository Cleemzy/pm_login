<h1><i class="bi bi-building"></i> Liste des sociétés</h1>

<table id="tb-auth">
  <thead>
    <tr>
      <th>Nom de la société</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for company <- @companies do %>
    <tr>
      <td><%= company.name %></td>

      <td>
        <span><a href="<%= Routes.company_path(@conn, :show, company) %>"><i class="bi bi-info-circle btn btn-info btn-lg" style="font-size: 20px;"></i></a></span>
        <span><a href="<%= Routes.company_path(@conn, :edit, company) %>"><i class="bi bi-pencil-square btn btn-warning btn-lg" style="font-size: 20px;"></i></a></span>
        <!-- <span><%= link "Delete", to: Routes.company_path(@conn, :delete, company), method: :delete, data: [confirm: "Are you sure?"] %></span> -->
      </td>
    </tr>
<% end %>
  </tbody>
</table>
<span><a href="<%= Routes.company_path(@conn, :new) %>" class="btn btn-lg btn-primary"><i class="bi bi-plus-circle"></i> Nouvelle société</a></span>

<!-- <span><%= link " +  Nouvelle société", to: Routes.company_path(@conn, :new),class: "btn btn-primary btn-lg" %></span> -->
